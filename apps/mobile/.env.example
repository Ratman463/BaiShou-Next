# 开发时 Metro 地址（手机要能访问）。
# - 开 Clash/VPN 时若自动识别成 198.18.x，请填电脑局域网 IP（如 192.168.x.x）
# - WSL2 下不要用 localhost；填 Windows 在局域网中的 IP（hostname -I 里的 192.168.x.x）
# REACT_NATIVE_PACKAGER_HOSTNAME=192.168.31.59
